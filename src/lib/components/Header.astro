<div class="bg-hero p-8 min-w-full flex flex-col items-center prose-lg">
    <h1 class="flex items-center gap-2 mb-0"> 
      <Image
        src={uofg}
        alt="University of Glasgow"
        class="size-16"
      />
      University of Glasgow
    </h1>
    <p>{"{{ ring_description }}"}</p>
  </div>